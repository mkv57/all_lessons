/* 10. Минимальная длина и вместимость слайса: Создайте слайс с
минимально возможной длиной и вместимостью, выведите слайс, его
длину и вместимость. */

package main

import "fmt"

func main() {

	a := []int{}

	fmt.Println(a, len(a), cap(a))
}
