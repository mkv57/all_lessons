/* 3. Функция, возвращающая значение: Создайте функцию, которая
принимает два числа и возвращает их произведение. */

package main

import "fmt"

func mkv(a, b int) int {

	return a * b
}

func main() {

	fmt.Println(mkv(2, 5))
}
